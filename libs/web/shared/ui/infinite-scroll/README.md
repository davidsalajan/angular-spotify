# web-shared-ui-infinite-scroll

This library provides the `InfiniteScrollDirective` (`[asInfiniteScroll]`) which uses `IntersectionObserver` to detect when a sentinel element becomes visible and emits a `scrolledToBottom` event.
